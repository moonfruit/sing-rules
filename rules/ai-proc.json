{
  "version": 1,
  "rules": [
    {
      "process_name": [
        "AltServer",
        "ChatGPT",
        "ChatGPTHelper",
        "Claude",
        "Claude Helper",
        "Claude Helper (GPU)",
        "Claude Helper (Renderer)"
      ]
    },
    {
      "process_path_regex": "/Applications/(ChatGPT|Claude)\\.app/.*"
    }
  ]
}
